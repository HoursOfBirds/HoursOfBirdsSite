Three.js 0.185.0 (MIT), https://threejs.org/

Vendored from the official npm distribution. Only the WebGL build, GLTFLoader,
CSS3DRenderer and their utility dependencies are included. Addon imports of
`three` are rewritten to the relative local build path, so production needs
neither a CDN nor an inline import map. See LICENSE for the upstream license.
